//version control application 

//commit 
version

Repository 

working repository (add) staging area -(commit) - local repository  (push) Remote repository 

Remote repository 

Gitbug desktop 


git add = working repo  -> staging area 
git commit -m "msg" staging area -> local repo
git push - local repor to remote repo 